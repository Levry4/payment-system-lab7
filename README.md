Лабораторная работа 7: Архитектура, слои и DDD-lite

Реализация системы оплаты заказа с использованием слоистой архитектуры и DDD-lite подхода.

📁 Структура проекта

Domain Layer (Доменный слой)

entities.py - доменные сущности:

Order - корневая сущность (агрегат)
OrderLine - часть агрегата Order
OrderStatus - перечисление статусов заказа
InvalidOrderOperation - исключение для недопустимых операций
value_objects.py:

Money - value object для денежных сумм
Application Layer (Слой приложения)

use_cases.py:

PayOrderUseCase - основной use-case оплаты заказа
OrderRepository - интерфейс репозитория
PaymentGateway - интерфейс платежного шлюза
PayOrderResult - DTO результата операции
Infrastructure Layer (Инфраструктурный слой)

repositories.py - InMemoryOrderRepository (in-memory реализация)
gateways.py - FakePaymentGateway (фейковый платежный шлюз)
Tests (Тесты)

test_use_cases.py - модульные тесты всех use-case и доменных инвариантов
Main

main.py - точка входа с полной демонстрацией работы системы
🎯 Реализованные требования

Доменные инварианты

 Нельзя оплатить пустой заказ - проверка в методе Order.pay()
 Нельзя оплатить заказ повторно - проверка статуса заказа
 После оплаты нельзя менять строки заказа - блокировка в add_line()
 Итоговая сумма равна сумме строк - автоматический расчет в Order.total
Use Case оплаты заказа

PayOrderUseCase.execute() выполняет:

Загрузку заказа через OrderRepository
Доменную операцию оплаты (order.pay())
Вызов платежа через PaymentGateway
Сохранение обновленного заказа
Возврат результата оплаты
Принципы DDD-lite

Агрегаты - Order является агрегатом для OrderLine
Value Objects - Money реализован как неизменяемый объект
Инварианты - бизнес-правила инкапсулированы в сущностях
Слоистая архитектура - четкое разделение ответственности
🚀 Запуск проекта

bash
# Установка зависимостей
pip install pytest

# Запуск демонстрации (рекомендуется сначала)
python main.py

# Запуск тестов
python -m pytest tests/ -v

# Или через unittest
python -m unittest discover tests/
